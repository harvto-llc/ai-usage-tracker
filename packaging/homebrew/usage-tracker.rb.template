cask "ai-cur-desktop" do
  version "__VERSION__"
  sha256 "__SHA256__"

  url "__URL__"
  name "ai-cur desktop client"
  desc "Claude and Codex quota, activity, and estimated-value menu bar with its local backend"
  homepage "https://github.com/harvto-llc/ai-usage-tracker"

  depends_on macos: ">= :sonoma"

  app "Usage Tracker.app"

  uninstall quit: "com.harvto.aicur.desktop"

  zap trash: [
    "~/.usage-tracker",
    "~/Library/Preferences/com.harvto.aicur.desktop.plist",
  ]
end
